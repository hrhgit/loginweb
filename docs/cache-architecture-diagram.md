# 缓存架构图

## 🏗️ 缓存系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        应用层 (Vue Components)                    │
├─────────────────────────────────────────────────────────────────┤
│  EventsPage  │ EventDetailPage │ TeamDetailPage │ ProfilePage   │
│  MyEventsPage│ SubmissionPage  │ TeamCreatePage │ ...           │
└─────────────────┬───────────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────────┐
│                   Vue Query Composables 层                      │
├─────────────────────────────────────────────────────────────────┤
│ useEvents()     │ useTeams()      │ useSubmissions()            │
│ useUsers()      │ useJudges()     │ useNotifications()          │
└─────────────────┬───────────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────────┐
│                    Vue Query 缓存层                             │
├─────────────────────────────────────────────────────────────────┤
│  Query Cache    │  Mutation Cache │  Error Cache               │
│  ┌─────────────┐│  ┌─────────────┐│  ┌─────────────┐           │
│  │ Events      ││  │ Create      ││  │ Network     │           │
│  │ Teams       ││  │ Update      ││  │ Validation  │           │
│  │ Submissions ││  │ Delete      ││  │ Permission  │           │
│  │ Users       ││  │ Join        ││  │ Server      │           │
│  │ Judges      ││  │ Invite      ││  └─────────────┘           │
│  │ Notifications││ └─────────────┘│                            │
│  └─────────────┘│                 │                            │
└─────────────────┬───────────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────────┐
│                   缓存管理层                                     │
├─────────────────────────────────────────────────────────────────┤
│ Memory Manager  │ Cache Optimizer │ Performance Monitor        │
│ ┌─────────────┐ │ ┌─────────────┐ │ ┌─────────────┐           │
│ │ GC Control  │ │ │ Auto Clean  │ │ │ Metrics     │           │
│ │ Size Limit  │ │ │ Age-based   │ │ │ Thresholds  │           │
│ │ Cleanup     │ │ │ Type-based  │ │ │ Alerts      │           │
│ └─────────────┘ │ └─────────────┘ │ └─────────────┘           │
└─────────────────┬───────────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────────┐
│                   存储层                                         │
├─────────────────────────────────────────────────────────────────┤
│ Browser Memory  │ IndexedDB       │ Browser Cache              │
│ ┌─────────────┐ │ ┌─────────────┐ │ ┌─────────────┐           │
│ │ Query Data  │ │ │ Offline     │ │ │ Static      │           │
│ │ Mutations   │ │ │ Queue       │ │ │ Assets      │           │
│ │ Errors      │ │ │ Sync State  │ │ │ Images      │           │
│ └─────────────┘ │ └─────────────┘ │ └─────────────┘           │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 数据流向图

```
用户操作
    │
    ▼
┌─────────────┐    缓存命中     ┌─────────────┐
│ Vue组件请求  │ ──────────────▶ │ 立即返回数据  │
└─────────────┘                └─────────────┘
    │                               │
    │ 缓存未命中/过期                  │
    ▼                               │
┌─────────────┐                     │
│ 发起网络请求  │                     │
└─────────────┘                     │
    │                               │
    ▼                               │
┌─────────────┐    后台更新     ┌─────▼─────┐
│ Supabase API│ ──────────────▶ │ 更新缓存   │
└─────────────┘                └───────────┘
    │                               │
    │ 请求失败                        │
    ▼                               │
┌─────────────┐                     │
│ 错误处理     │                     │
│ 重试机制     │                     │
└─────────────┘                     │
    │                               │
    └───────────────────────────────┘
```

## 🎯 缓存策略矩阵

| 数据类型 | 实时性 | 缓存时间 | GC时间 | 使用场景 |
|---------|--------|---------|--------|----------|
| **通知数据** | 高 | 10秒 | 5分钟 | 实时通知 |
| **活动列表** | 中 | 30秒 | 15分钟 | 浏览活动 |
| **队伍数据** | 中 | 30秒 | 15分钟 | 组队功能 |
| **作品数据** | 中 | 30秒 | 15分钟 | 作品展示 |
| **用户资料** | 低 | 5分钟 | 30分钟 | 个人信息 |
| **评委权限** | 低 | 5分钟 | 30分钟 | 权限控制 |
| **公开活动** | 低 | 2分钟 | 30分钟 | 首页展示 |

## 🔧 缓存键命名规范

```
queryKeys = {
  // 一级分类
  events: {
    all: ['events'],                           // 所有活动
    public: ['events', 'public'],              // 公开活动
    my: (userId) => ['events', 'my', userId],  // 我的活动
    detail: (id) => ['events', 'detail', id],  // 活动详情
  },
  
  teams: {
    all: ['teams'],                                    // 所有队伍
    byEvent: (eventId) => ['teams', 'event', eventId], // 活动队伍
    members: (teamId) => ['teams', 'members', teamId], // 队伍成员
    seekers: (eventId) => ['teams', 'seekers', eventId], // 求组队
    myMemberships: (userId) => ['teams', 'memberships', userId],
    myRequests: (userId) => ['teams', 'my-requests', userId],
    myInvites: (userId) => ['teams', 'my-invites', userId],
  },
  
  submissions: {
    all: ['submissions'],                                      // 所有作品
    byEvent: (eventId) => ['submissions', 'event', eventId],   // 活动作品
    byTeam: (teamId) => ['submissions', 'team', teamId],       // 队伍作品
  },
  
  user: {
    all: ['user'],                                    // 所有用户
    profile: (userId) => ['user', 'profile', userId], // 用户资料
    contacts: (userId) => ['user', 'contacts', userId], // 联系方式
    registrations: (userId) => ['user', 'registrations', userId], // 报名记录
  },
  
  judges: {
    all: ['judges'],                                           // 所有评委
    byEvent: (eventId) => ['judges', 'event', eventId],        // 活动评委
    permissions: (eventId, userId) => ['judges', 'permissions', eventId, userId], // 评委权限
  },
  
  notifications: {
    all: ['notifications'],                                    // 所有通知
    byUser: (userId) => ['notifications', 'user', userId],     // 用户通知
  },
}
```

## 📊 内存使用分布

```
总内存使用 (目标: <30MB)
├── Vue Query 缓存 (60%) ~18MB
│   ├── Events 数据 (25%) ~4.5MB
│   ├── Teams 数据 (30%) ~5.4MB
│   ├── Submissions 数据 (20%) ~3.6MB
│   ├── Users 数据 (15%) ~2.7MB
│   ├── Judges 数据 (5%) ~0.9MB
│   └── Notifications 数据 (5%) ~0.9MB
├── 性能监控 (15%) ~4.5MB
├── 错误处理 (10%) ~3MB
├── 网络状态 (5%) ~1.5MB
└── 其他开销 (10%) ~3MB
```

## 🚀 缓存优化流程

```
启动应用
    │
    ▼
初始化Vue Query
    │
    ▼
设置缓存配置
    │
    ▼
启动内存管理器 ──┐
    │           │
    ▼           │
启动缓存优化器 ──┤
    │           │
    ▼           │
启动性能监控 ────┤
    │           │
    ▼           │
开始缓存监控 ────┘
    │
    ▼
┌─────────────────┐
│ 运行时优化循环   │
│ ┌─────────────┐ │
│ │ 每2分钟检查  │ │
│ │ 内存使用情况 │ │
│ └─────────────┘ │
│ ┌─────────────┐ │
│ │ 自动清理     │ │
│ │ 过期缓存     │ │
│ └─────────────┘ │
│ ┌─────────────┐ │
│ │ 性能监控     │ │
│ │ 和报告       │ │
│ └─────────────┘ │
└─────────────────┘
```

## 🎛️ 调试工具架构

```
开发者工具
├── 浏览器控制台
│   ├── __VUE_QUERY_DEBUG__.getCacheStats()
│   ├── __VUE_QUERY_DEBUG__.getMemoryStats()
│   ├── __VUE_QUERY_DEBUG__.optimizeCache()
│   └── analyzeCacheUsage()
├── Vue DevTools
│   ├── 组件状态查看
│   ├── 查询状态监控
│   └── 性能分析
├── 浏览器开发者工具
│   ├── Network 标签 (网络请求)
│   ├── Memory 标签 (内存使用)
│   └── Performance 标签 (性能分析)
└── 自动监控
    ├── 控制台日志输出
    ├── 性能警告
    └── 内存使用报告
```

这个架构图清楚地展示了项目中缓存系统的完整结构，从应用层到存储层的各个组件以及它们之间的关系。